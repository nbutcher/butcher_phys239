All text and figures for the homework can be generated by running 

bash run.bash

If that does not work, run

python generate_ic.py
python driver.py

The final intensity plots are labeled A through F corresponding the the test
case listed. The column density and cross sections are printed into the
terminal window. Plots for the cross section as asked for in 3 will be labeled
by the maximum cross section.
